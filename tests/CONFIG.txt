# number of cpus assigned for experiment >= 2. Must be same as configured during TimeKeeper installation.
N_CPUS = 4						

# number of serial devices per PLC
NR_SERIAL_DEVS  = 3												

# Tx Buffer size per serial device
TX_BUF_SIZE = 100													

# directory containing Timekeeper code
TIMEKEEPER_DIR = /home/user/Desktop/TimeKeeper
